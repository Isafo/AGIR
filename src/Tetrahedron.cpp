#include "Tetrahedron.h"

Tetrahedron::Tetrahedron(const glm::vec3 p, float dX, float dY, float dZ) {

	/*m_t1 = std::make_unique<Triangle>
		(
			glm::vec3(-dX / 2.0f, -dY / 2.0f, dZ / 2.0f),
			glm::vec3(0.0f, -dY / 2.0f, -dZ / 2.0f),
			glm::vec3(dX / 2.0f, -dY / 2.0f, dZ / 2.0f)
		);

	m_t2 = std::make_unique<Triangle>
		(
			glm::vec3(-dX / 2.0f, -dY / 2.0f, dZ / 2.0f),
			glm::vec3(dX / 2.0f, -dY / 2.0f, dZ / 2.0f),
			glm::vec3(0.0f, dY / 2.0f, 0.0f)
		);

	m_t3 = std::make_unique<Triangle>
		(
			glm::vec3(dX / 2.0f, -dY / 2.0f, dZ / 2.0f),
			glm::vec3(0.0f, -dY / 2.0f, -dZ / 2.0f),
			glm::vec3(0.0f, dY / 2.0f, 0.0f)
		);

	m_t4 = std::make_unique<Triangle>
		(
			glm::vec3(0.0f, -dY / 2.0f, -dZ / 2.0f),
			glm::vec3(-dX / 2.0f, -dY / 2.0f, dZ / 2.0f),
			glm::vec3(0.0f, dY / 2.0f, 0.0f)
		);*/
}